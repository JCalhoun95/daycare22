<h1>Kids index</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Age</th>
      <th>Description</th>
      <th>Action</th>
    </tr>
  </thead>

  <tbody>
    <% @kids.each do |kids| %>
    <tr>
      <td><%= kids.name%></td>
      <td><%= kids.age%></td>
      <td><%= kids.description%></td>   
      <td><%= link_to 'Show', kids_path(kids) %> </td>
      <td><%= link_to 'Edit', kids_path(kids) %> </td>
      <td><%= link_to 'Delete', kids_path(kids), method: :delete %> </td>
 
      <td><%= link_to 'Create Kid', new_path(kids) %> </td>
    </tr>
    <% end %>
  </tbody>
</table>